---
title: 재귀함수 쿼리
type: resource
tags:
  - query
created: '2025-11-30'
updated: '2025-11-30'
aliases: []
status: seedling
maturity: 0
---

## 🔍 쿼리 전체 설명

```sql
WITH RECURSIVE generate_fridays AS (
    SELECT  '2023-11-01'::DATE AS day
    UNION ALL
    SELECT (day + INTERVAL '1 day')::DATE
    FROM generate_fridays
    WHERE day < '2023-11-30'::DATE
)
SELECT *
FROM generate_fridays;

```

---

## ✅ 파트별 설명

### 1. WITH RECURSIVE generate_fridays AS (...)

- RECURSIVE 키워드는 자기 자신을 참조해서 재귀적으로 데이터를 생성할 때 사용
- 여기선 11월 1일부터 11월 30일까지의 모든 날짜 목록을 생성하는 데 사용
---

### 2. Anchor 쿼리 (초기값)

```sql
sql
복사편집
SELECT '2023-11-01'::DATE AS day

```

- 재귀 시작 지점
- '2023-11-01' 문자열을 DATE로 형변환하여 day라는 컬럼으로 시작
- 즉, 첫 번째 행은 2023-11-01
---

### 3. Recursive 쿼리 (반복 파트)

```sql
sql
복사편집
SELECT (day + INTERVAL '1 day')::DATE
FROM generate_fridays
WHERE day < '2023-11-30'::DATE

```

- 이전에 생성된 generate_fridays.day 값에 하루씩 더해가며 새로운 날짜 생성
- day + INTERVAL '1 day' : 하루 더하기
- 종료 조건: day < '2023-11-30' → 11월 30일까지 반복
즉, 2023-11-01 → 2023-11-02 → … → 2023-11-30까지 총 30개의 날짜가 생성

---

## 📎 Related

<!-- 자동 생성된 섹션 - 수동으로 링크를 추가하세요 -->

### Projects

### Knowledge

### Insights

