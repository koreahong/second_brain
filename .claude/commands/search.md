Activate the Search Agent from `.claude/agents/search-agent.md`.

Query: {{user_message_after_command}}

Use multi-modal search:
1. Semantic search (AI embedding)
2. Tag-based search
3. Graph traversal
4. Temporal search
5. Frequency-based

Return top 10 results with relevance scores and reasons.
