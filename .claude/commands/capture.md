Activate the Capture Agent from `.claude/agents/capture-agent.md`.

Capture the following idea/thought/note and create a Fleeting Note in `00-Inbox/`.

User input: {{user_message_after_command}}

Follow the Capture Agent workflow exactly.
